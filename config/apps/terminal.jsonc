{
  // Windows Terminal profile.
  //
  // VibeOS suppresses Windows' VK_GAMEPAD_* translation while Active, so
  // Terminal's native XAML pad nav never sees the pad and our bindings are
  // the only effect — no double-drive, no suspend dance. (While Suspended
  // the pad behaves stock and Terminal drives itself natively.)
  "name": "Terminal",
  "match": {
    "process": ["WindowsTerminal.exe", "wt.exe"]
  },
  "bindings": {
    "RB+A": { "key": "CTRL+SHIFT+T" },
    "RB+DpadRight": "next-tab",
    "RB+DpadLeft": "prev-tab",
    "RB+X": { "key": "CTRL+SHIFT+F" }
  },
  "wheel": {
    "name": "Terminal",
    "slots": [
      { "label": "New tab", "key": "CTRL+SHIFT+T" },
      { "label": "Next tab", "action": "next-tab" },
      { "label": "Prev tab", "action": "prev-tab" },
      { "label": "Find", "key": "CTRL+SHIFT+F" },
      { "label": "Copy", "action": "copy" },
      { "label": "Paste", "action": "paste" },
      { "label": "Select all", "action": "select-all" },
      { "label": "Desktop", "action": "show-desktop" }
    ]
  },
  "voice": {
    "mode": "instant"
  }
}
